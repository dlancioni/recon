### Important commands 
# build exe
pyinstaller.exe --onefile --icon=icon.ico --name recon main.py

# install dependencies
pip install -r requirements.txt

# test application
python -m unittest discover -stest -v -b

# recon calls
python main.py --f "test datatype (pt-br)"
python main.py --f "en-us"
python main.py --f "volume 10k"